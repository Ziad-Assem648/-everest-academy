{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix user && npm --prefix user run build && npm install --prefix admin/frontend && npm --prefix admin/frontend run build && npm install --prefix admin/backend"
  },
  "deploy": {
    "startCommand": "node admin/backend/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
